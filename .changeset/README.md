# Changesets

Run `yarn changeset` whenever you make a release-relevant change to the published package.

- `develop` creates beta prerelease version PRs and publishes to the `beta` dist-tag.
- `master` creates stable version PRs and publishes to the `latest` dist-tag.
